import React from "react";
import OSMath from '../providers/MathProvider';

handleDays = (base_days) => {
  axios.get(``)
    .then( res => {
      
    })
};

handleMultiplier = (multiplier) => {
  axios.get(``)
    .then( res => {

    })
};




export default OSMath;